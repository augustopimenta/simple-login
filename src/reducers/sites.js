const initialState = {
    current: null,
    list: [
        { domain: "", users: [] }
    ]
};

export default (state = initialState, action) => {
    switch (action.type) {

    }

    return state;
};